
<div class="d-flex justify-content-center gap-3">
    {{ form_start(form) }}
        <div class="mt-4">
            {{ form_widget(form) }}
            <button class="btn bg-info mt-4">{{ button_label|default('Enregistrer') }}</button>
        </div>
    {{ form_end(form) }}
</div>